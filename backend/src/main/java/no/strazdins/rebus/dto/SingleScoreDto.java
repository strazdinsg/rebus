package no.strazdins.rebus.dto;

/**
 * A DTO object for transferring single score property (for a single answer).
 *
 * @param score The score for the answer
 */
public record SingleScoreDto(Integer score) {
}
